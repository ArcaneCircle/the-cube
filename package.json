{
  "private": true,
  "packageManager": "pnpm@6.32.3",
  "scripts": {
    "dev": "vite --host --open",
    "prebuild": "vite build && sass src/scss:assets/css",
    "build": "jszip-cli -c jszip.config.json",
    "css": "sass --watch src/scss:assets/css",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@ffflorian/jszip-cli": "^3.1.6",
    "sass": "^1.51.0",
    "vite": "^2.9.9"
  }
}
